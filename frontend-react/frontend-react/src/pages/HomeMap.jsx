import { useState } from "react";

export default function HomeMap() {
  const [menuOpen, setMenuOpen] = useState(false);

  return (
    <div>
      <h1>HOME MAP FUNCIONANDO ♻️</h1>
    </div>
  );
}